#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point of app
 * 0x01-variables_if_else_while
 * Return: 0
 */
int main(void)
{
	for (int i = 97; i <= 122; ++i)
	{
		putchar(i);
	}
	for (int i = 65; i <= 90; ++i)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
