#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point of app
 * 0x01-variables_if_else_while
 * Return: 0
 */
int main(void)
{
	for (int i = 48; i <= 57; ++i)
	{
		putchar(i);
	}
	for (char i = 'a'; i <= 'f'; ++i)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
