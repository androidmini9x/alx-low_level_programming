#include "main.h"
/**
 * mul - Checks for uppercase character
 * @a: int - first number
 * @b: int - second number
 * Return: 1 if is digit, otherwise 0
 */
int mul(int a, int b)
{
	return (a * b);
}
